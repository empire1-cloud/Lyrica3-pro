import React from 'react';

/**
 * MusicEngine — Lyrica3 Pro music generation panel (stub)
 * Full implementation wires to backend /api/generate endpoint via useStudioStore.
 */
export default function MusicEngine() {
  return (
    <div className="flex flex-col items-center justify-center h-full text-muted-foreground">
      <p className="text-sm">Music Engine — coming soon</p>
    </div>
  );
}
