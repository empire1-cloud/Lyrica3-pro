{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "build",
  "env": {
    "REACT_APP_BACKEND_URL": "http://localhost:8000",
    "REACT_APP_GEMINI_API_KEY": "placeholder"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
