# Lyrica 3 frontend
# Local same-origin proxy: /api
# Separate backend deployment: use the full backend URL ending in /api
VITE_BACKEND_URL=/api

# Must match LYRICA_FULL_RUNTIME_V1_ENABLED on the backend.
VITE_LYRICA_FULL_RUNTIME_V1=false
